# Diabetes-Detection-
A machine learning project for diabetes detection using a structured healthcare dataset, with data preprocessing, EDA, and classification models such as Logistic Regression, KNN, SVM, and Random Forest.
